/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configurações padrão do Next.js
  // Não usar 'export' pois precisamos de servidor para Firebase Admin
}

module.exports = nextConfig